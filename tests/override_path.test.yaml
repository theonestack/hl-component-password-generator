test_metadata:
  type: config
  name: override_path
  description: set the description for your test

test_parameters:
  MyPathSuffix: mypath

ssm_path:
  Fn::Sub: "/${Identifier}/${EnvironmentName}/${MyPathSuffix}"